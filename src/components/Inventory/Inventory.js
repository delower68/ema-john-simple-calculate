import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h1>This is only for Inventory part.
                not yet complete.
            </h1>
        </div>
    );
};

export default Inventory;